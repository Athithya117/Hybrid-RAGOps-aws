# Ignore everything not needed in Docker context
# Speeds up builds and protects secrets

# Python virtual environments
.venv/
.venv2/
.venv1/
.venv_refined/
venv/
myenv/

# Python cache
__pycache__/
*.pyc
repos
production-stack
# Data directories
data/
raw_data/
rag8s-aws/
rag8s-dev/

# Large or sensitive data
models/
lid.176.bin
backups/
images/
tmp/

# Secrets and config
02-secrets.yml
commands.md
cli.md
tmp.md

# Git and versioning
.git/
.gitignore
.dockerignore

# Development files not needed in Docker image
notebooks/
*.zip

# ---- Markdown Rule Fix ----
# Do NOT ignore all markdown files — allow README.md, docs, etc.
# If you need to ignore some md files, list them explicitly above
