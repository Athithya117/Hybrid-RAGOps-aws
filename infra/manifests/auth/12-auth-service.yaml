apiVersion: v1
kind: Service
metadata:
  name: forward-auth-svc
  namespace: inference-auth
spec:
  type: ClusterIP
  ports:
  - port: 9000
    targetPort: 9000
    protocol: TCP
    name: http
  selector:
    app: forward-auth
