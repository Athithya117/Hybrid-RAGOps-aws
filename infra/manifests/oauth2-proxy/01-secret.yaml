apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-secrets
  namespace: auth
stringData:
  cookie-secret: REPLACE_ME_COOKIE_SECRET
  google-client-id: REPLACE_ME_GOOGLE_CLIENT_ID
  google-client-secret: REPLACE_ME_GOOGLE_CLIENT_SECRET
  azure-client-id: REPLACE_ME_AZURE_CLIENT_ID
  azure-client-secret: REPLACE_ME_AZURE_CLIENT_SECRET
type: Opaque
