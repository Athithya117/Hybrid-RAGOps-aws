apiVersion: v1
kind: Service
metadata:
  name: clickhouse-exporter
  namespace: observability
spec:
  selector:
    app: clickhouse
  ports:
  - name: metrics
    port: 9116
    targetPort: 9116
  type: ClusterIP
