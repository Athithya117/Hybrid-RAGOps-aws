apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: inference
spec:
  replicas: 1
  selector:
    matchLabels: &id001
      app.kubernetes.io/name: frontend
  template:
    metadata:
      labels: *id001
      annotations:
        frontend/config-checksum: 5337438ffccc025fe134ffd27ee25fbea0a90e7749aee4ab4b39007383b91179
    spec:
      serviceAccountName: frontend-sa
      containers:
      - name: frontend
        image: athithya5354/frontend-and-auth:v10
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: frontend-config
        env:
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: frontend-secret
              key: JWT_SECRET
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              name: frontend-secret
              key: SESSION_SECRET
        - name: GOOGLE_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: frontend-secret
              key: GOOGLE_CLIENT_SECRET
        - name: MS_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: frontend-secret
              key: MS_CLIENT_SECRET
        - name: GITHUB_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: frontend-secret
              key: GITHUB_CLIENT_SECRET
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /orchestrator/health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 1
        readinessProbe:
          httpGet:
            path: /orchestrator/health
            port: 8000
          initialDelaySeconds: 3
          periodSeconds: 5
          timeoutSeconds: 1
