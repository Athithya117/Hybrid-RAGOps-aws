apiVersion: v1
kind: Service
metadata:
  name: retrieval-svc
  namespace: inference
  labels:
    app.kubernetes.io/name: retrieval
    app.kubernetes.io/component: inference
    app.kubernetes.io/managed-by: retriever-generator
    app.kubernetes.io/instance: retrieval
    env: staging
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: retrieval
